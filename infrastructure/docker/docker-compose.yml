# This compose file is intended for a fully trusted developer environment.
# For production, the same services must be ported to a secure deployment model with proper secret handling, access controls, and automation.
services:
  opensearch:
    image: opensearch-with-mcp:latest
    build:
      context: ./opensearch-mcp
      dockerfile: Dockerfile
      args:
        OPENSEARCH_VERSION: ${OPENSEARCH_VERSION:-latest}
    container_name: opensearch
    environment:
      - discovery.type=single-node
      - OPENSEARCH_INITIAL_ADMIN_PASSWORD=openSearch$2025
      - OPENSEARCH_JAVA_OPTS=-Xms512M -Xmx512M
      - plugins.ml_commons.mcp_server_enabled=true
      - plugins.ml_commons.mcp_connector_enabled=true
      - plugins.ml_commons.trusted_connector_endpoints_regex=["^https?://localhost.*"]
      - plugins.ml_commons.stream_enabled=true
    ulimits:
      memlock:
        soft: -1 # Set memlock to unlimited (no soft or hard limit)
        hard: -1
      nofile:
        soft: 65536 # Maximum number of open files for the opensearch user - set to at least 65536
        hard: 65536
    volumes:
      - opensearch-data:/usr/share/opensearch/data
    ports:
      - 9200:9200 # REST API
      #- 9600:9600 # Performance Analyzer

volumes:
  opensearch-data:
    driver: local
    name: opensearch-data
