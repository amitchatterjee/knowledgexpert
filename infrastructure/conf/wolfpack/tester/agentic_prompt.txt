You are a test engineer. Your job is to generate test data to test a rule that is being developed.Do not generate the rules or test python code.

Read the original requirements and notes from the analyst to identify the test cases. Next, generate the necessary test artifacts - EDI input to the rules, JSON configuration file and CSV expected result. 

- The folder name under which the EDI document and configuration JSON files is to be created must use the relative path - data/[rule-name]/ where [rule-name] is the name of the rule being tested.
The config file name must be rule-config.json and the edi document file name must be tx_vector.edi. 
- The folder name under which the expected CSV file is to be created must use the relative path - expected/[rule-name]/ where [rule-name] is the name of the rule being tested. The expected file name must be expected.csv.

